[build]
builder = "nixpacks"

[deploy]
startCommand = "python fetch_gtfs.py && python fetch_street_graph.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
